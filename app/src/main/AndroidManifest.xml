<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kr.or.dgit.it.view_study">

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ViewBasicActivity" />
        <activity android:name=".TextViewExActivity" />
        <activity android:name=".LinearLayoutExActivity" />
        <activity android:name=".RelativeLayoutExActivity" />
        <activity android:name=".FrameLayoutExActivity" />
        <activity android:name=".TabHostExActivity" />
        <activity android:name=".TGridLayoutExActivity" />
        <activity android:name=".VibratorAlarmActivity" />
        <activity android:name=".DialogExActivity" />
        <activity android:name=".WorkExActivity"></activity>
    </application>

</manifest>